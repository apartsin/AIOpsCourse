# -*- coding: utf-8 -*-
"""Generate weekly lesson-plan pages for Modern AI Systems: Development,
Deployment, and Operations. Each week -> lessons/weekNN.html with a detailed
2 h lecture timeline and a detailed 2 h practice timeline, in the DLCourseHIT
house style (assets/style.css).
v3: course renamed; weekly labs dropped; every week ends with a
project-integration brief (the running project is the single deliverable);
timelines are fully detailed (8 lecture segments, 7 practice segments)."""
import os, html

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
OUT = os.path.join(ROOT, "lessons")
os.makedirs(OUT, exist_ok=True)

BRAND = "Modern AI Systems"
FOOT = ("Modern AI Systems: Development, Deployment &amp; Operations "
        "&middot; HIT &middot; Advanced Course &middot; course materials")

def esc(s): return html.escape(s, quote=False)

# Each timeline row: (clock, minutes, head, detail)  detail = str | list[str] | None
WEEKS = [
{
 "n":1,"part":"Part I &middot; Foundations &amp; the Cloud",
 "title":"Production Engineering & the Ops Landscape",
 "obj":["Explain why production systems fail in operations more than in modelling.",
        "Define SLIs, SLOs, and error budgets and write one of each.",
        "Map the five operational layers (DevOps through AgentOps) and how they stack.",
        "Name the course use-case domains and shortlist one for the team project."],
 "lec":[("0:00-0:10",10,"Welcome & objectives",
            ["Course mechanics: 2 h lecture + 2 h practice, one running project, three presentations (weeks 5, 8, 13), no written exams.",
             "What this course is: the engineering around the model, not the model itself.",
             "This week's objectives on the board."]),
        ("0:10-0:25",15,"Motivation: the prototype-to-production gap",
            ["A walk through a real incident: an accurate model, a stale feature pipeline, a silent 40% error rate for three weeks.",
             "The 90/10 inversion: the model is a small box in a large systems diagram.",
             "Discussion prompt: what could have caught this, and at which layer?"]),
        ("0:25-0:50",25,"Reliability as a contract: SLIs, SLOs, error budgets",
            ["SLI: a measured indicator (request latency, error rate, freshness).",
             "SLO: the target on that indicator; SLA: the contract with consequences.",
             "Error budgets: a quantified allowance of unreliability to spend on change.",
             "Board work: turn 'the chatbot should be fast and reliable' into two SLOs with numbers.",
             "The toil concept and why automation is an SRE obligation, not a luxury."]),
        ("0:50-1:10",20,"What 'production-ready' means",
            ["Availability, latency (p95, not average), cost, recoverability, observability.",
             "Day-one versus day-two: shipping is the start, operating is the job.",
             "The on-call loop: detect, triage, mitigate, learn (blameless postmortems).",
             "Quick poll: which property does your favourite app most visibly fail on?"]),
        ("1:10-1:20",10,"Break",None),
        ("1:20-1:40",20,"The five operational layers",
            ["DevOps: code to running service (CI/CD, containers, observability).",
             "DataOps: trustworthy data (pipelines, quality, versioning).",
             "MLOps: the model lifecycle (tracking, registry, serving, drift).",
             "LLMOps: operating models you did not train (APIs, RAG, evaluation, cost).",
             "AgentOps: operating loops that act (tools, tracing, bounds).",
             "The stacking argument: each layer inherits the guarantees, and the debts, of those below."]),
        ("1:40-1:55",15,"The course use cases & the running project",
            ["IoT telemetry: sensor streams, anomaly alerts, predictive maintenance.",
             "Document-QA chatbot: RAG over a real corpus, grounded answers with citations.",
             "Document processing: structured extraction from invoices and forms at scale.",
             "How one system will thread all five layers across thirteen weeks; what each Student Project Presentation must show."]),
        ("1:55-2:00",5,"Wrap-up & practice preview","Revisit the checks below; practice sets up Git, Docker, and the team repositories.")],
 "miss":("If the model is accurate, the system is done.",
         "Accuracy is one property. Availability, latency, cost, data freshness, and recoverability are separate properties that usually dominate production outcomes."),
 "checks":[("Give one failure a 99%-accurate model can still cause in production.",
            "It can serve at 5 s latency, crash under load, or be fed stale or garbage inputs. Accuracy says nothing about any of these."),
           ("What does an error budget let a team do?",
            "Spend a quantified amount of unreliability on shipping features, and freeze risky changes once it is exhausted. It makes reliability a negotiable, measurable resource.")],
 "take":["Production failures are mostly operational, not algorithmic.",
         "SLOs and error budgets turn 'reliability' into a measurable contract.",
         "The five layers compose; each later layer inherits the earlier ones."],
 "prac":[("0:00-0:10",10,"Setup & recap",
            ["Verify Git, Docker, and an editor on every machine; fix stragglers now.",
             "Recap: SLOs, error budgets, the five layers, the three use cases."]),
        ("0:10-0:35",25,"Git for teams, done properly",
            ["Branch, commit, push, pull request, review, merge: the full loop live.",
             "Branch protection and required reviews on the shared repository.",
             "Repository layout for a service: src, tests, infra, docs; .gitignore and secrets hygiene from day one."]),
        ("0:35-1:00",25,"Containers from zero",
            ["Write a Dockerfile for a minimal 'hello-service' line by line.",
             "Build, run, stop, and inspect; read the logs; map the port.",
             "Pin the base image and dependency versions; rebuild and show identical behaviour."]),
        ("1:00-1:10",10,"Break",None),
        ("1:10-1:35",25,"Team formation & project kickoff",
            ["Form teams of three or four; create each team's repository from the course template.",
             "Walk the template: service skeleton, infra folder, README contract.",
             "Each team opens its first pull request (the README) and merges it through review."]),
        ("1:35-1:50",15,"Students drive",
            ["Each team containerises the template service and runs it locally.",
             "Instructor circulates; common failures (port clashes, missing pins) fixed live."]),
        ("1:50-2:00",10,"Project-integration brief","Walk the 'Project integration' card below: what each team adds to its system before next week and how it builds toward Presentation 1.")],
 "proj":["Create the team repository from the template, with branch protection and a clean layout.",
         "Containerise the hello-service and run it locally; pin all versions.",
         "Shortlist two use-case domains (IoT, chatbot, document processing, or a proposed alternative); decide by week 3."],
 "pit":["Never commit secrets or large data to Git; use .gitignore and environment variables from day one.",
        "A container that runs only on your laptop usually pins no versions. Pin them."],
},
{
 "n":2,"part":"Part I &middot; Foundations &amp; the Cloud",
 "title":"Cloud Computing Fundamentals",
 "obj":["Explain the core cloud primitives: compute, storage, and networking.",
        "Choose between IaaS, PaaS, SaaS, and serverless for a given workload.",
        "Reason about regions, availability zones, shared responsibility, and cost.",
        "Provision the project's cloud footprint on a free tier, safely."],
 "lec":[("0:00-0:10",10,"Recap & objectives",
            ["Recall: SLOs and the five layers; two retrieval questions on week 1.",
             "Today: the platform everything runs on. No prior cloud experience assumed."]),
        ("0:10-0:25",15,"Motivation: the bill and the blast radius",
            ["Every production AI system runs on someone's cloud; the design decisions are rent-versus-build decisions.",
             "Two stories: a forgotten GPU instance that cost a month's budget in a weekend, and a single-AZ outage that took a 'redundant' service down.",
             "Cost and failure domains are design inputs from day one."]),
        ("0:25-0:50",25,"Compute: from metal to functions",
            ["Virtual machines: a rented computer; you own the OS upward.",
             "Containers on managed runtimes: you own the image, the platform runs it.",
             "Serverless functions: you own only code and configuration; cold starts and time limits in exchange for zero idle cost.",
             "The control-versus-toil axis as the single picture to remember.",
             "Board work: place the three course use cases on that axis and defend the placement."]),
        ("0:50-1:10",20,"Storage & managed services",
            ["Object storage (the S3 model): cheap, durable, URL-addressable; the default home for datasets, models, logs, and the data lake to come in week 5.",
             "Block storage belongs to one VM; file shares for legacy POSIX needs.",
             "Managed databases and queues: paying the provider to carry backups, patching, and failover.",
             "Rule of thumb: artifacts and data in object storage, state in a managed database, glue in functions."]),
        ("1:10-1:20",10,"Break",None),
        ("1:20-1:40",20,"Networking, regions & the deployment models",
            ["Virtual networks, subnets, security groups; load balancers and DNS; HTTPS termination.",
             "Regions and availability zones: what multi-AZ protects against, and what it does not.",
             "IaaS versus PaaS versus SaaS named precisely; where serverless sits.",
             "The shared-responsibility model: the provider secures the cloud, you secure what is in it."]),
        ("1:40-1:55",15,"The cost model (predict, then run)",
            ["Pay-as-you-go: compute time, storage volume, requests, and the one everyone forgets: egress.",
             "Live exercise: the class predicts the monthly cost of the chatbot use case at 10k requests/day, then we price it together in the calculator.",
             "Budgets, alerts, and tagging as non-negotiable hygiene."]),
        ("1:55-2:00",5,"Wrap-up & practice preview","Practice provisions the project's real cloud footprint, with a budget alert set before anything else.")],
 "miss":("The cloud is just someone else's computer, so nothing really changes.",
         "Elasticity, managed services, per-use billing, and the shared-responsibility split change how systems are designed, costed, and secured. A design that ignores them is either fragile or expensive."),
 "checks":[("You need to store trained model artifacts and serve them to many machines. Object, block, or file storage, and why?",
            "Object storage: cheap, durable, URL-addressable, and built for many readers. Block storage belongs to one VM; file shares scale poorly for this."),
           ("What failure does deploying across two availability zones protect against?",
            "The loss of a single data center (power, cooling, network). It does not protect against a region-wide outage or a bad deploy you ship to both zones.")],
 "take":["Compute, storage, and networking primitives compose into any architecture.",
         "The deployment model (IaaS to serverless) is a control-versus-toil trade-off.",
         "Cost and blast radius are design inputs; budgets and multi-AZ are decisions, not defaults."],
 "prac":[("0:00-0:10",10,"Setup & recap",
            ["Cloud free-tier accounts confirmed for every team.",
             "Recap the primitives and the control-versus-toil axis."]),
        ("0:10-0:35",25,"First things first: budgets, then a VM",
            ["Set a budget alert on every team account before provisioning anything.",
             "Launch a small VM, connect to it, and run the week-1 container on it.",
             "Read the instance metadata: region, zone, type, hourly price."]),
        ("0:35-1:00",25,"Object storage for the project",
            ["Create the team bucket that will carry the project's data and artifacts all semester.",
             "Upload and download from code (not the console): the SDK pattern.",
             "Show URL-addressability and access control; why this bucket becomes the data lake in week 5."]),
        ("1:00-1:10",10,"Break",None),
        ("1:10-1:35",25,"Same app, three deployment models",
            ["Deploy the hello-service on the VM (IaaS), then as a managed container/PaaS app, then as a serverless function.",
             "Compare: effort to deploy, cold start, cost at zero traffic, control retained.",
             "Each team records the comparison table; it becomes part of the Presentation-1 architecture rationale."]),
        ("1:35-1:50",15,"Students drive: price your project",
            ["Each team prices its shortlisted use case at target load in the calculator.",
             "Instructor reviews the two most expensive line items per team."]),
        ("1:50-2:00",10,"Project-integration brief","Teardown discipline demonstrated live; then the 'Project integration' card: the team's cloud footprint must exist, budgeted and torn down clean, before next week.")],
 "proj":["Create the team's cloud space with a budget alert as the first action.",
         "Provision the project bucket (the future data lake) and access it from code.",
         "Deploy the hello-service on the free tier in at least two deployment models; record the comparison for Presentation 1; tear down what is not needed."],
 "pit":["A forgotten running instance silently burns the free tier; set budget alerts first and tear down last.",
        "Never put cloud credentials in code or notebooks; use roles and environment configuration."],
},
{
 "n":3,"part":"Part II &middot; DevOps",
 "title":"CI/CD, Testing & REST Services",
 "obj":["Build a CI pipeline that lints, tests, builds, and publishes an artifact.",
        "Apply the testing pyramid to a service.",
        "Design, version, and containerise a REST API.",
        "Commit the team to its project use case."],
 "lec":[("0:00-0:10",10,"Recap & objectives",
            ["Retrieval: object versus block storage; what multi-AZ buys.",
             "Today: making change safe, and shipping the interface everything else will call."]),
        ("0:10-0:25",15,"Motivation: deploys that break on Friday",
            ["The manual-deploy horror story: works on one machine, fails in production, no one can reproduce either.",
             "DORA's evidence in one slide: elite teams deploy more often with fewer failures because of automation, not in spite of it.",
             "The feedback-loop argument: small changes, fast verification, cheap rollback."]),
        ("0:25-0:50",25,"Continuous integration & the testing pyramid",
            ["What CI actually is: integrating small changes into a shared mainline continuously, with automated verification at every push.",
             "Pipeline anatomy: stages, jobs, artifacts, caches; what runs on every commit versus on merge.",
             "The pyramid: many fast unit tests, fewer integration tests, very few end-to-end; why the inverted pyramid (the 'ice-cream cone') fails.",
             "Trunk-based development versus long-lived branches: integration pain grows with branch age.",
             "Continuous delivery versus continuous deployment; where a human approval belongs."]),
        ("0:50-1:10",20,"Infrastructure as code, briefly but properly",
            ["Declarative versus imperative provisioning; the Terraform model of desired state.",
             "Idempotency: applying the same configuration twice converges, never stacks.",
             "Environment parity: dev, staging, and production from one definition.",
             "Where IaC lives in the project: the infra folder is reviewed like code because it is code."]),
        ("1:10-1:20",10,"Break",None),
        ("1:20-1:40",20,"REST services done properly",
            ["Resources, verbs, and status codes; idempotent verbs and why retries care.",
             "Request/response contracts in JSON; validation at the boundary.",
             "Versioning an API (path or header) so consumers survive evolution; deprecation as a managed process.",
             "Health and readiness endpoints: what the orchestrator will probe next week."]),
        ("1:40-1:55",15,"Live demo (predict, then run)",
            ["Predict: what happens when a breaking change ships without an API version bump? Then we ship one against a running client and watch it break.",
             "Re-ship the same change as v2 alongside v1; the old client survives.",
             "The lesson: the contract is the product."]),
        ("1:55-2:00",5,"Wrap-up & practice preview","Practice builds the project's real service skeleton and the pipeline that guards it.")],
 "miss":("CI is just running the tests.",
         "CI is the discipline of integrating small changes into a shared mainline continuously, with automated verification. The test run is one stage of that, not the whole idea."),
 "checks":[("Why are end-to-end tests the thin top of the pyramid, not the base?",
            "They are slow, flaky, and expensive. Most coverage should come from fast unit tests, with fewer integration tests and very few end-to-end."),
           ("What does versioning an API buy you?",
            "Consumers keep working when the API evolves: old clients stay on v1 while v2 changes the contract, and deprecation becomes a managed process instead of a breakage.")],
 "take":["CI integrates small changes continuously; the test run is one stage.",
         "Push coverage down the pyramid: fast unit tests are the base.",
         "An API contract is a promise; version it and guard it with tests."],
 "prac":[("0:00-0:10",10,"Setup & recap",
            ["Open the team repositories; confirm the use-case decision is due today.",
             "Recap pipeline anatomy and the pyramid."]),
        ("0:10-0:35",25,"The project service skeleton",
            ["Build a FastAPI service with two real endpoints for the chosen use case plus /health.",
             "Pydantic models as the request/response contract; validation rejects bad input at the boundary.",
             "Wire the service to read configuration from the environment, never from code."]),
        ("0:35-1:00",25,"Tests that earn their place",
            ["Unit tests for the core logic; an integration test that boots the service and exercises an endpoint.",
             "Watch the pyramid in miniature: 12 unit, 3 integration, 1 end-to-end.",
             "Break the contract on purpose; watch the integration test catch it."]),
        ("1:00-1:10",10,"Break",None),
        ("1:10-1:35",25,"The pipeline that guards the mainline",
            ["GitHub Actions workflow: lint, test, build, image push to the registry, on every push.",
             "Dependency caching and a build matrix; keep the loop under five minutes.",
             "Required checks: a failing test now physically blocks the merge."]),
        ("1:35-1:50",15,"Students drive",
            ["Each team lands one endpoint + test + green pipeline through a reviewed pull request.",
             "Instructor circulates on pipeline failures."]),
        ("1:50-2:00",10,"Project-integration brief","The 'Project integration' card: the service skeleton and its pipeline are this week's increment and the backbone of everything that follows.")],
 "proj":["Commit to the project use case (IoT, chatbot, document processing, or approved alternative).",
         "Stand up the project's REST API skeleton: two real endpoints, /health, contract validation, tests.",
         "CI pipeline live: lint, test, build, image push; required checks gate every merge from now on."],
 "pit":["Flaky tests erode trust in CI; quarantine and fix them rather than re-running.",
        "A 40-minute pipeline kills the feedback loop; parallelise and cache."],
},
{
 "n":4,"part":"Part II &middot; DevOps",
 "title":"Orchestration, Deployment Patterns & Observability",
 "obj":["Run the project service under an orchestrator with health checks and scaling.",
        "Choose and execute a rollout pattern (blue-green, canary).",
        "Instrument the three pillars of observability and read a RED dashboard."],
 "lec":[("0:00-0:10",10,"Recap & objectives",
            ["Retrieval: the testing pyramid; why version an API.",
             "Today: from one container to a managed fleet, and seeing what it does."]),
        ("0:10-0:25",15,"Motivation: 3 a.m. and the pod is gone",
            ["One container is easy; forty containers with deploys, crashes, and traffic spikes are why orchestration exists.",
             "Story: the deploy that took down checkout because all instances restarted at once; the rollout pattern that would have saved it.",
             "You cannot operate what you cannot see: the observability half of today."]),
        ("0:25-0:50",25,"Kubernetes: the mental model",
            ["Desired state and reconciliation: you declare, the control loop converges.",
             "Pods, Deployments, Services; labels and selectors as the glue.",
             "Liveness and readiness probes: the /health endpoint from week 3 finds its purpose.",
             "Resource requests and limits; horizontal autoscaling.",
             "Board work: trace a request from DNS through the Service to a pod, and what happens when that pod dies."]),
        ("0:50-1:10",20,"Deployment patterns & GitOps",
            ["Rolling update: the default, and its window of mixed versions.",
             "Blue-green: two full environments, one switch, instant rollback, double cost.",
             "Canary: a small slice of real traffic watched closely, then ramp; the pattern the project will use for models too.",
             "GitOps in one picture: the cluster state lives in Git, an agent (Argo CD) reconciles it; the audit log is the repo history."]),
        ("1:10-1:20",10,"Break",None),
        ("1:20-1:40",20,"Observability: logs, metrics, traces",
            ["The three pillars and the question each answers: what happened, how much, and where in the chain.",
             "Monitoring answers known questions; observability lets you ask new ones without shipping code.",
             "Structured logs (JSON) versus grep archaeology; high-cardinality labels.",
             "The RED method: rate, errors, duration, per service; USE for resources."]),
        ("1:40-1:55",15,"Tail latency (predict, then run)",
            ["Predict: average latency is 80 ms; what is p99, and who experiences it?",
             "Run a live load test; watch p50 stay flat while p99 explodes under saturation.",
             "Why SLOs are written on percentiles, never averages."]),
        ("1:55-2:00",5,"Wrap-up & practice preview","Practice deploys the project to a cluster, runs a canary, and builds the dashboard the rest of the course reads.")],
 "miss":("Observability just means more dashboards.",
         "Observability is the ability to ask new questions of a running system without shipping new code. Well-structured, high-cardinality telemetry, not the number of dashboards, is what enables it."),
 "checks":[("What is the difference between blue-green and canary deployment?",
            "Blue-green swaps all traffic between two full environments at once; canary shifts a small percentage gradually and watches metrics before ramping up."),
           ("When do traces help where aggregate metrics do not?",
            "Traces follow a single request across services and show where its latency is spent, which an aggregate metric cannot localise.")],
 "take":["Orchestration reconciles desired state across a fleet.",
         "Canary shifts traffic gradually and watches metrics; blue-green flips all at once.",
         "Tail latency (p95/p99) and traces reveal what averages hide."],
 "prac":[("0:00-0:10",10,"Setup & recap",
            ["Start a local cluster (kind or minikube) on every team machine.",
             "Recap: desired state, probes, RED."]),
        ("0:10-0:35",25,"Deploy the project service",
            ["Write the Deployment and Service manifests for the week-3 service.",
             "Wire the liveness and readiness probes to /health.",
             "Scale to three replicas; kill a pod; watch reconciliation bring it back."]),
        ("0:35-1:00",25,"Canary rollout, for real",
            ["Ship v2 of the service next to v1; shift 10% of traffic.",
             "Watch the error rate per version; promote, then practice the rollback path.",
             "This exact pattern returns in week 7 for models."]),
        ("1:00-1:10",10,"Break",None),
        ("1:10-1:35",25,"The project dashboard",
            ["Expose Prometheus metrics from the service; scrape them.",
             "Build the RED dashboard in Grafana: rate, errors, p50/p95/p99 duration.",
             "Run the load test; read the tail; record the baseline p95 for the project log."]),
        ("1:35-1:50",15,"Students drive",
            ["Each team gets its service deployed, canaried, and on the dashboard.",
             "Instructor circulates on probe and scrape misconfigurations."]),
        ("1:50-2:00",10,"Project-integration brief","The 'Project integration' card: deployed service + canary path + RED dashboard; the baseline p95 goes into the Presentation-1 spec.")],
 "proj":["Deploy the project service to a cluster with health probes and three replicas.",
         "Demonstrate a canary rollout and a rollback on the project service.",
         "Stand up the project RED dashboard; record baseline p95 latency for the spec."],
 "pit":["p50 latency hides the tail; always watch p95/p99.",
        "Unstructured logs are hard to query; emit structured (JSON) logs."],
},
{
 "n":5,"part":"Part III &middot; DataOps",
 "title":"Data Lakes, Pipelines & Versioning",
 "obj":["Explain the data lake, the lakehouse, and the medallion architecture.",
        "Build an orchestrated data pipeline with retries and backfills.",
        "Version a dataset and reproduce a result from a pinned snapshot."],
 "lec":[("0:00-0:10",10,"Recap & objectives",
            ["Retrieval: canary versus blue-green; what RED measures.",
             "Today: the data side begins; presentations this afternoon."]),
        ("0:10-0:25",15,"Motivation: garbage in, production out",
            ["Most ML incidents trace to data, not the model: a schema change upstream, a silent duplication, a timezone shift.",
             "Data needs an architecture, a refinery, and version control, exactly like code.",
             "The project bucket from week 2 becomes a real data lake today."]),
        ("0:25-0:50",25,"From warehouse to lake to lakehouse",
            ["The warehouse: structured, governed, expensive; the lake: cheap object storage, schema-on-read, easily a swamp.",
             "The lakehouse: lake storage plus table semantics (Delta, Iceberg): ACID writes, schema enforcement, time travel.",
             "Why object storage from week 2 is the substrate for all of it.",
             "One-slide tour of the engines above the formats: Spark for heavy transforms, dbt for SQL-shaped ones."]),
        ("0:50-1:10",20,"The medallion architecture",
            ["Bronze: raw, immutable, as-ingested; the audit trail of what actually arrived.",
             "Silver: cleaned, de-duplicated, conformed; the trustworthy middle.",
             "Gold: aggregated, business- and feature-ready; what models and dashboards consume.",
             "Ownership and quality expectations per layer; why 'clean it later' becomes 'trust it never'.",
             "Board work: the IoT use case as bronze sensor events, silver de-duplicated readings, gold hourly features per device."]),
        ("1:10-1:20",10,"Break",None),
        ("1:20-1:40",20,"Pipelines: orchestration done right",
            ["DAGs, scheduling, and dependencies in Airflow or Dagster.",
             "Idempotency: re-running a task must converge, not double-write.",
             "Retries, backfills, and parameterised run dates; never hardcode 'today'.",
             "Batch versus streaming, previewed; week 6 goes deeper."]),
        ("1:40-1:55",15,"Data versioning & lineage",
            ["DVC and lakeFS: immutable snapshots, pointers in Git, data stays in the lake.",
             "Reproducing a result means pinning code SHA and data version together; week 7 builds on exactly this.",
             "Lineage: tracing a gold number back through silver and bronze to the source."]),
        ("1:55-2:00",5,"Wrap-up & Student-Project-Presentation logistics","Final reminders for Student Project Presentation 1: running order, timing, and what the rubric rewards.")],
 "miss":("A data lake is a dump; structure can be added later for free.",
         "Without explicit zones and contracts the lake becomes a swamp nobody trusts. The medallion architecture makes refinement stages, quality expectations, and ownership explicit from the first byte."),
 "checks":[("What lives in each medallion layer?",
            "Bronze: raw, immutable, as-ingested events. Silver: cleaned, de-duplicated, conformed records. Gold: aggregated, business- or feature-ready tables that models and dashboards consume."),
           ("Why must an orchestrated task be idempotent?",
            "Retries and backfills re-execute tasks. A non-idempotent task double-writes or corrupts state when it runs twice.")],
 "take":["The lakehouse is lake storage plus table semantics; medallion stages the refinement.",
         "Most ML production bugs are data bugs.",
         "Pinning a data version is what makes a result reproducible."],
 "prac":None,
 "proj":["Apply the feedback from Presentation 1 to the specification.",
         "Define the project's medallion layout and land the first real data in bronze.",
         "Initialise data versioning (DVC) on the project bucket; commit the first snapshot pointer."],
 "pit":["A specification without measurable SLOs is not a specification; name the numbers.",
        "Hardcoding 'today' in a pipeline breaks backfills; parameterise the run date."],
 "pres":"Student Project Presentation 1 &middot; Specification",
 "present":["Problem statement and success metrics (SLOs) for the chosen use case.",
            "System and data architecture, including the planned medallion layout and cloud footprint (with the week-2 deployment-model comparison).",
            "DevOps status: repository, CI pipeline, deployed skeleton, baseline p95 from the RED dashboard.",
            "Risk and governance register."],
},
{
 "n":6,"part":"Part III &middot; DataOps",
 "title":"Data Quality, Contracts, Streaming & Feature Stores",
 "obj":["Enforce data quality continuously with validation rules.",
        "Define a data contract between a producer and a consumer.",
        "Explain streaming ingestion (Kafka) and where it beats batch.",
        "Explain feature stores and the cause of train/serve skew."],
 "lec":[("0:00-0:10",10,"Recap & objectives",
            ["Retrieval: the three medallion layers; why idempotency.",
             "Today: keeping the data trustworthy, and keeping it fresh."]),
        ("0:10-0:25",15,"Motivation: the silent schema change",
            ["Story: an upstream team renames a column on a Tuesday; the model degrades for three weeks with zero errors thrown.",
             "Quality failures are silent by default; loud is something you must engineer.",
             "Some use cases also cannot wait for tonight's batch: the freshness half of today."]),
        ("0:25-0:50",25,"Data quality, engineered",
            ["Validation as code (Great Expectations): schema, ranges, nullity, uniqueness, referential checks.",
             "Where to validate: at the bronze-to-silver boundary, the gate you control.",
             "Fail loudly versus quarantine: when each is right; the quarantine table pattern.",
             "Quality SLAs: freshness, completeness, validity, with numbers and owners.",
             "Profiling first: you cannot write expectations for data you have not looked at."]),
        ("0:50-1:10",20,"Data contracts",
            ["The contract: schema, semantics, freshness, quality, and a change process the producer guarantees.",
             "Enforcement at the boundary, automatically; a contract nobody checks is a wish.",
             "Consumer-driven contracts: the consumer's tests run against the producer's changes.",
             "Board work: draft the contract for the project's main source, live."]),
        ("1:10-1:20",10,"Break",None),
        ("1:20-1:40",20,"Streaming: Kafka in one lecture",
            ["Topics, partitions, producers, consumers, consumer groups; the log as the core abstraction.",
             "Delivery semantics: at-least-once as the default reality; idempotent consumers (week 5's lesson returns).",
             "When streaming beats batch: value decays faster than the batch period (sensor alarms, fraud); when batch wins: simplicity and cost.",
             "The IoT use case end to end: device, topic, bronze landing, silver conformance."]),
        ("1:40-1:55",15,"Feature stores & train/serve skew",
            ["Offline features for training, online features for serving; one definition, two materialisations.",
             "Point-in-time correctness: the subtle leak that inflates offline metrics.",
             "Train/serve skew: a transformation that exists only in the training notebook; why it is silent and how the feature store removes the duplication."]),
        ("1:55-2:00",5,"Wrap-up & practice preview","Practice gates the project's real data and lands a live stream in the lake.")],
 "miss":("Data validation is a one-time cleaning step.",
         "Quality is a continuous contract checked on every run. Upstream producers keep changing, so validation must guard the boundary forever, not once."),
 "checks":[("What is train/serve skew and one cause?",
            "The features seen at serving differ from those at training, e.g. a transformation applied only in the training notebook, or time leakage in offline features."),
           ("When does streaming genuinely beat batch?",
            "When the value of the data decays faster than the batch period: fraud signals, sensor alarms, live personalisation. If a nightly aggregate is fine, batch is simpler and cheaper.")],
 "take":["Data quality is a continuous contract, not a one-off clean.",
         "Streams are the freshness lever; pay their complexity only when freshness pays.",
         "Train/serve skew is a top cause of silent model failure."],
 "prac":[("0:00-0:10",10,"Setup & recap",
            ["Open the project's bronze data from week 5.",
             "Recap: boundaries, contracts, the log."]),
        ("0:10-0:35",25,"Profile, then gate",
            ["Profile the project's bronze data live; find the real dirt (nulls, duplicates, ranges).",
             "Write Great Expectations suites at the bronze-to-silver boundary.",
             "Break the data on purpose; watch the gate fail loudly and route rows to quarantine."]),
        ("0:35-1:00",25,"The contract, in force",
            ["Write the one-page data contract for the project's main source.",
             "Wire the contract checks into the pipeline so a violation fails the run.",
             "Simulate the upstream rename from the lecture story; watch it get caught in seconds, not weeks."]),
        ("1:00-1:10",10,"Break",None),
        ("1:10-1:35",25,"A stream into the lake",
            ["Stand up single-broker Kafka; create the topic.",
             "Produce a simulated sensor stream (IoT teams: your real feed); consume and land it in bronze.",
             "Run the silver transform on top; the medallion now has a live inlet."]),
        ("1:35-1:50",15,"Students drive",
            ["Each team gets its gate, contract, and (where applicable) stream running on its own data.",
             "Instructor circulates on expectation tuning."]),
        ("1:50-2:00",10,"Project-integration brief","The 'Project integration' card: gates, contract, and the corpus or stream landed; the data side is now Presentation-2 ready.")],
 "proj":["Validation gates live at the bronze-to-silver boundary, with a tested quarantine path.",
         "The data contract for the main source written and enforced in the pipeline.",
         "IoT teams: live stream landing in bronze; chatbot and document teams: the corpus feed landed and conformed to silver.",
         "Gold feature tables defined with one shared definition for training and serving (the lightweight feature-store pattern)."],
 "pit":["Validate at the boundary you do not control, not only your own outputs.",
        "A consumer that silently trusts the producer's schema is the first thing to break."],
},
{
 "n":7,"part":"Part IV &middot; MLOps",
 "title":"Experiment Tracking, Model Registry & Serving",
 "obj":["Track experiments so any result is reproducible.",
        "Version models in a registry with governed stage transitions.",
        "Serve a model behind a REST endpoint and roll it out safely."],
 "lec":[("0:00-0:10",10,"Recap & objectives",
            ["Retrieval: train/serve skew; when streaming wins.",
             "Today: the model lifecycle, from training run to live endpoint."]),
        ("0:10-0:25",15,"Motivation: which run made this model?",
            ["Story: the production model whose training data, code version, and hyperparameters nobody could name during an audit.",
             "Training is the easy part; versioning, serving, and safely replacing models is where production ML lives.",
             "Everything today reuses weeks 3 to 5: the API contract, the canary, the data version."]),
        ("0:25-0:50",25,"Experiment tracking, properly",
            ["What to log: parameters, metrics over time (curves, not just finals), artifacts, environment.",
             "The reproducibility triple: git SHA + data version (week 5's DVC snapshot) + environment, pinned to every run.",
             "Comparing runs in the MLflow UI; reading a learning curve versus a final number.",
             "Tracking applies to LLM work too: prompt versions and eval scores are runs (weeks 9 to 10 will reuse this)."]),
        ("0:50-1:10",20,"The model registry & model cards",
            ["A model version is an artifact plus its lineage, not a file on a laptop; the registry doubles as the model catalogue: discovery, ownership, and governance in one place.",
             "Registry stages: staging, production, archived; promotion as a governed, auditable transition.",
             "Rollback as a first-class operation: the previous version is one transition away.",
             "Model cards: intended use, training data, metrics, limitations; documentation that ships with the artifact."]),
        ("1:10-1:20",10,"Break",None),
        ("1:20-1:40",20,"Serving patterns",
            ["Packaging: the model plus pinned dependencies, containerised like any service.",
             "Online (request/response), batch (precomputed), and streaming scoring; choosing by freshness need and cost.",
             "The serving endpoint is a REST API: week 3's contracts, validation, and health checks apply unchanged.",
             "Latency engineering: model warm-up, request batching, the p95 budget."]),
        ("1:40-1:55",15,"Safe rollout for models (predict, then run)",
            ["Shadow: real traffic, responses unused; the free lunch of rollout safety.",
             "Canary and A/B for models; what online metric decides promotion.",
             "Predict: offline AUC up 2 points; what can still go wrong online? Then the offline-online gap, demonstrated with a latency regression."]),
        ("1:55-2:00",5,"Wrap-up & practice preview","Practice takes the project model from tracked run to canaried endpoint.")],
 "miss":("A good offline metric guarantees a good online result.",
         "Offline metrics use historical data and proxy objectives. Feedback loops, latency, and distribution shift mean online behaviour can differ, so validate with shadow, canary, or A/B."),
 "checks":[("What three things must a tracked run pin to be reproducible?",
            "The code version (git SHA), the data version, and the environment plus hyperparameters."),
           ("What is a shadow deployment?",
            "Sending real traffic to the new model in parallel without using its responses, to compare it against production safely.")],
 "take":["A metric is reproducible only when tied to code, data, and environment.",
         "A registry gives governed, auditable model promotion and rollback.",
         "Offline metrics are a proxy; confirm online with shadow or canary."],
 "prac":[("0:00-0:10",10,"Setup & recap",
            ["Open the project's training script and the MLflow UI.",
             "Recap the reproducibility triple."]),
        ("0:10-0:35",25,"Instrument and track",
            ["Instrument the project's training: params, metric curves, artifacts.",
             "Pin the git SHA and the DVC data version to the run; show the lineage end to end.",
             "Train twice with different hyperparameters; compare runs in the UI."]),
        ("0:35-1:00",25,"Register, promote, roll back",
            ["Register the better run as model v1; write its model card from the run metadata.",
             "Promote staging to production; then practice the rollback transition.",
             "Chatbot and document teams: the 'model' may be a prompt + model-tier configuration; track it the same way."]),
        ("1:00-1:10",10,"Break",None),
        ("1:10-1:35",25,"Serve it",
            ["Wrap the registered model in a serving runtime behind the project's REST API.",
             "Health probes, input validation, and warm-up; deploy to the cluster from week 4.",
             "Load-test; read p95 on the RED dashboard; compare to the recorded baseline."]),
        ("1:35-1:50",15,"Canary the model",
            ["Ship model v2 as a canary next to v1, exactly like week 4's service canary.",
             "Compare per-version predictions and latency; promote or roll back on evidence."]),
        ("1:50-2:00",10,"Project-integration brief","The 'Project integration' card: tracked, registered, served, canaried; the model lifecycle is now demonstrable for Presentation 2.")],
 "proj":["Project training instrumented with MLflow; reproducibility triple pinned on every run.",
         "Model v1 registered with a model card; promotion and rollback demonstrated.",
         "Model served behind the project's REST API on the cluster, with p95 recorded; v2 canaried next to v1."],
 "pit":["An unpinned data version makes the run impossible to reproduce.",
        "Serving a pickled model without pinned library versions breaks silently."],
},
{
 "n":8,"part":"Part IV &middot; MLOps",
 "title":"Monitoring, Model Drift & Governance",
 "obj":["Monitor a deployed model and its inputs.",
        "Distinguish data drift from concept drift and detect each.",
        "Define retraining triggers and an audit trail."],
 "lec":[("0:00-0:10",10,"Recap & objectives",
            ["Retrieval: the reproducibility triple; shadow versus canary.",
             "Today: the day-two problem; presentations this afternoon."]),
        ("0:10-0:25",15,"Motivation: the model that rotted quietly",
            ["Story: a recalibrated sensor fleet shifts the input distribution; accuracy follows six weeks later, after the damage.",
             "Models decay because the world moves; the question is how early you can know.",
             "Labels are late or absent in production, so accuracy is usually unobservable in real time."]),
        ("0:25-0:50",25,"Drift, precisely",
            ["Data drift: P(X) changes; the inputs no longer look like training.",
             "Concept drift: P(Y|X) changes; the world's mapping itself moved.",
             "Label shift, feature drift, prediction drift: what is observable when.",
             "Why input drift is the early-warning signal: it needs no labels and precedes the accuracy drop.",
             "The IoT case on the board: which drift is the recalibrated sensor, and what would you see first?"]),
        ("0:50-1:10",20,"Detectors and their trade-offs",
            ["Population stability index and KS tests on features; thresholds and their false-alarm rates.",
             "Embedding-distance drift for unstructured inputs (text, images): the detector the chatbot teams need.",
             "Prediction-distribution monitoring and proxy metrics when labels lag.",
             "Window choices: reference versus rolling; seasonality returns in week 12."]),
        ("1:10-1:20",10,"Break",None),
        ("1:20-1:40",20,"Retraining & governance",
            ["Retraining triggers: schedule, drift threshold, proxy degradation; cost of each false trigger.",
             "The retraining loop reuses everything: pipeline (week 5), tracking and registry (week 7), canary (weeks 4, 7).",
             "Human-in-the-loop approval for promotion; the audit trail an auditor will actually ask for.",
             "Every detector pairs with a documented action, or it is decoration."]),
        ("1:40-1:55",15,"Worked example (predict, then run)",
            ["Predict: we shift one feature's distribution by 15%; which detector fires first, PSI or KS?",
             "Run the simulation; read the detectors; decide, as a class, whether the retraining trigger should fire."]),
        ("1:55-2:00",5,"Wrap-up & Student-Project-Presentation logistics","Final reminders for Student Project Presentation 2: the live CI/CD demo is mandatory; running order posted.")],
 "miss":("If accuracy looks fine, there is no drift.",
         "Labels are often delayed, so live accuracy is usually unknown. Input drift can be detected before any accuracy drop and is the real early-warning signal."),
 "checks":[("What is the difference between data drift and concept drift?",
            "Data drift is a change in the input distribution; concept drift is a change in the input-to-output relationship itself."),
           ("What do you monitor when ground-truth labels arrive weeks late?",
            "Input and feature distributions and the prediction distribution as proxies, alongside business KPIs.")],
 "take":["Models decay; monitoring is a day-two necessity, not optional.",
         "Input drift is detectable before accuracy drops.",
         "A detector without an attached action is just noise."],
 "prac":None,
 "proj":["Apply the feedback from Presentation 2.",
         "Stand up the input-drift monitor on the project's served model (PSI or embedding distance, per use case).",
         "Write the retraining-trigger policy: which signal, which threshold, which action, who approves."],
 "pit":["A drift alarm with no playbook is noise; attach an action to every detector.",
        "Watching only accuracy misses drift while labels are delayed."],
 "pres":"Student Project Presentation 2 &middot; Interim",
 "present":["Working data pipeline through bronze and silver, with validation gates and the data contract in force.",
            "Experiment tracking and a versioned model (or tracked prompt configuration) in the registry.",
            "The model served behind the project's REST API, with the canary path and the RED dashboard shown live.",
            "CI/CD demonstrated live: a change lands through the gated pipeline during the talk.",
            "Monitoring and drift plan for the second half."],
},
{
 "n":9,"part":"Part V &middot; LLMOps &amp; AgentOps",
 "title":"LLM Foundations: AI APIs, Tokens & the Token Economy",
 "obj":["Explain what an LLM does, from an engineering standpoint.",
        "Call an AI API well: prompts, parameters, structured outputs.",
        "Reason about tokens, context windows, pricing, and model tiers.",
        "Survey the managed AI services (Bedrock-class platforms)."],
 "lec":[("0:00-0:10",10,"Recap & objectives",
            ["Retrieval: data versus concept drift.",
             "Today: language models enter the system. No prior LLM experience assumed."]),
        ("0:10-0:25",15,"Motivation: the model you did not train",
            ["Most AI features today are built on a large model behind an API: you control neither weights nor updates.",
             "The engineering questions shift: not 'how was it trained' but cost, latency, reliability, and correctness of a dependency.",
             "Live teaser: the same prompt, run twice, gives two different answers; today explains why and what to do about it."]),
        ("0:25-0:50",25,"LLMs for engineers",
            ["Next-token prediction over a context window: the one sentence that explains most behaviour.",
             "Tokens and tokenization: the unit of cost, latency, and length limits; live tokenizer demo on Hebrew and English text.",
             "The API call anatomy: system prompt, user messages, temperature, stop conditions, max tokens.",
             "Sampling and nondeterminism: why temperature 0 reduces but does not eliminate variance.",
             "Context windows: what fits, what falls out, and what 'lost in the middle' means."]),
        ("0:50-1:10",20,"From free text to engineering-grade outputs",
            ["Why parsing free text is a trap: format drift breaks parsers silently.",
             "Structured outputs: JSON schemas and function signatures; the model fills a contract (week 3's lesson, again).",
             "Prompt patterns that matter in production: role, constraints, examples, output schema.",
             "Failure modes catalogued: hallucination, prompt sensitivity, instruction conflicts; live demo on the invoice use case."]),
        ("1:10-1:20",10,"Break",None),
        ("1:20-1:40",20,"The token economy",
            ["Pricing per input and output token; input is usually the volume driver in RAG-shaped systems.",
             "Estimating a feature's cost: tokens per request times requests per day; the spreadsheet every team fills today.",
             "The cost levers in order: shorter context, prompt caching, batching, cheaper tier, then and only then a different model.",
             "Rate limits and quotas; retries with backoff, and why naive retries double the bill.",
             "Latency anatomy: time-to-first-token versus tokens-per-second; what streaming changes for UX."]),
        ("1:40-1:55",15,"Managed AI services (Bedrock-class)",
            ["What the platforms bundle: model catalog, guardrails, knowledge bases, agents, evals, provisioned throughput.",
             "AWS Bedrock as the archetype; Microsoft Foundry and Vertex AI as the siblings; naming churn as a cautionary tale.",
             "Platform versus direct API: governance and one console versus control and portability.",
             "Where the gateway pattern (next week) sits between the two."]),
        ("1:55-2:00",5,"Wrap-up & practice preview","Practice makes first API calls, counts real tokens, and prices each team's feature.")],
 "miss":("An LLM API call behaves like a normal deterministic function.",
         "The same input can produce different outputs (sampling), the contract is tokens rather than characters, and the model behind the endpoint can change. Engineering around it needs structured outputs, retries with backoff, pinned model versions, and evaluation."),
 "checks":[("Why does a longer context cost more and respond slower?",
            "Billing is per token on both input and output, and attention computation grows with sequence length, so every extra token costs money and latency."),
           ("Name two levers that cut an LLM feature's cost before switching models.",
            "Shorten the prompt and context, cache repeated calls; batching requests and routing easy queries to a cheaper tier are next.")],
 "take":["The model API is the new runtime; cost and latency are token-driven.",
         "Structured outputs turn LLM calls into engineering-grade components.",
         "Managed platforms bundle catalog, guardrails, and RAG; direct APIs trade that for control."],
 "prac":[("0:00-0:10",10,"Setup & recap",
            ["API keys issued through the course proxy; never raw keys in code.",
             "Recap: tokens, temperature, structured outputs."]),
        ("0:10-0:35",25,"First calls, measured",
            ["First API calls (OpenAI or Anthropic SDK): system prompt, user prompt, temperature.",
             "Count tokens with the tokenizer; log latency and computed cost per call.",
             "Same prompt at temperature 0 and 1, five runs each; see the variance with your own eyes."]),
        ("0:35-1:00",25,"Structured extraction (document-processing use case)",
            ["Define a JSON schema for invoice fields; the model fills the contract.",
             "Feed a malformed document; handle the refusal/failure path explicitly.",
             "Validate the output against the schema at the boundary, like any API response (week 3)."]),
        ("1:00-1:10",10,"Break",None),
        ("1:10-1:35",25,"Tiers and the bill",
            ["Run the same extraction on a flagship and a mini model; compare quality, latency, and cost in a table.",
             "Fill the token-economy spreadsheet for the project's LLM feature at 10k requests/day.",
             "Find each team's biggest cost lever; usually it is context length."]),
        ("1:35-1:50",15,"Managed-platform tour",
            ["Instructor-led tour of a Bedrock-class console: catalog, guardrails, knowledge bases, evals.",
             "Map each console feature to the open-source counterpart the course teaches."]),
        ("1:50-2:00",10,"Project-integration brief","The 'Project integration' card: the project's LLM feature wired with structured outputs and a costed token-economy model.")],
 "proj":["Wire the project's LLM feature through the course proxy with structured outputs and schema validation.",
         "Produce the feature's token-economy model: cost per request and per month at target load, with the chosen tier justified.",
         "Pin the model version in configuration; add the retry-with-backoff wrapper."],
 "pit":["Never hardcode API keys; use environment configuration and the course proxy.",
        "Pin the model version: silent model updates change behaviour under you."],
},
{
 "n":10,"part":"Part V &middot; LLMOps &amp; AgentOps",
 "title":"RAG & Serving LLMs: Vector Databases & Gateways",
 "obj":["Build a retrieval-augmented generation service over a real corpus.",
        "Manage prompts as versioned program logic.",
        "Choose between hosted APIs and self-hosted serving (vLLM).",
        "Route all LLM traffic through a gateway with fallbacks and budgets."],
 "lec":[("0:00-0:10",10,"Recap & objectives",
            ["Retrieval: two cost levers; why pin model versions.",
             "Today: grounding the model in your data, and putting an operations layer in front of every call."]),
        ("0:10-0:25",15,"Motivation: the model has never read your documents",
            ["A model answers from training data; your corpus, your sensors, and your invoices are not in it.",
             "Fine-tuning versus retrieval: why retrieval wins for fresh, factual, citable knowledge.",
             "And once LLM calls are load-bearing: who handles a provider outage, a leaked key, a runaway bill? The gateway half of today."]),
        ("0:25-0:50",25,"RAG, end to end",
            ["Embeddings: text to vectors; semantic similarity as geometry.",
             "The vector database (FAISS, Qdrant): index, search, metadata filters.",
             "Chunking: size, overlap, structure; where most RAG quality is won or lost.",
             "Assembling the grounded prompt: retrieved context, citations, instruction discipline.",
             "The chatbot use case on the board: silver-layer corpus to index to retrieval to grounded answer."]),
        ("0:50-1:10",20,"Prompts as code",
            ["Prompts are program logic: a prompt change alters behaviour like a code change.",
             "Versioning, review, and rollback for prompts; tracked in MLflow like week 7's models.",
             "Prompt templates and variables; separating instructions from data (the injection defense begins here).",
             "Regression discipline previewed: next week every prompt change runs the evals."]),
        ("1:10-1:20",10,"Break",None),
        ("1:20-1:40",20,"The serving choice: hosted versus self-hosted",
            ["Hosted APIs: zero infrastructure, per-token cost, provider control of versions and limits.",
             "Self-hosted open weights with vLLM: control and unit economics at scale, in exchange for GPUs and ops burden.",
             "Throughput engineering in one slide: batching, KV cache, time-to-first-token.",
             "The decision rubric: volume, privacy, latency, and team capacity."]),
        ("1:40-1:55",15,"The gateway pattern (predict, then run)",
            ["One OpenAI-compatible endpoint, many providers behind it (LiteLLM): routing, fallbacks, key custody, budgets, logging.",
             "Predict: the primary provider goes down mid-request; what should the user see? Then we kill it live and watch the fallback.",
             "Where the gateway sits in every project architecture from today on."]),
        ("1:55-2:00",5,"Wrap-up & practice preview","Practice builds each project's RAG core and puts the gateway in front of it.")],
 "miss":("RAG means the model can no longer hallucinate.",
         "Retrieval grounds the answer, but the model can still ignore or misread the context. Faithfulness depends on retrieval quality, chunking, and citations, and must be measured rather than assumed."),
 "checks":[("Why version prompts like code?",
            "Prompts are program logic: a prompt change alters behaviour, so it must be tracked, reviewed, and rollback-able like any other code."),
           ("Give one reason RAG retrieval fails even with a good model.",
            "Poor chunking or an embedding mismatch means the relevant passage is never retrieved; a recall failure the generator cannot recover from.")],
 "take":["Retrieval quality bounds RAG quality; chunking is where it is won or lost.",
         "Prompts are versioned program logic, not free text.",
         "A gateway centralises routing, keys, fallbacks, and budgets for every LLM call."],
 "prac":[("0:00-0:10",10,"Setup & recap",
            ["Open the project corpus (silver layer) and the vector database.",
             "Recap: chunking, grounded prompts, the gateway."]),
        ("0:10-0:35",25,"Build the RAG core",
            ["Chunk and embed the corpus; index it with metadata.",
             "Retrieve for real questions; inspect what actually came back before blaming the model.",
             "Tune the chunking once and re-inspect: see retrieval quality move."]),
        ("0:35-1:00",25,"Ground and generate",
            ["Assemble the grounded prompt with citations; generate and read the answers critically.",
             "Store the prompt as a versioned file; register the version like a model artifact.",
             "Swap prompt versions live and diff the behaviour."]),
        ("1:00-1:10",10,"Break",None),
        ("1:10-1:35",25,"The gateway in front",
            ["Stand up LiteLLM with two providers; the project now calls one endpoint.",
             "Demonstrate fallback by killing the primary provider mid-demo.",
             "Set a per-team budget cap and a logging hook; the bill is now observable."]),
        ("1:35-1:50",15,"Students drive",
            ["Each team gets retrieval running on its own corpus behind the gateway.",
             "Instructor circulates on chunking and embedding mismatches."]),
        ("1:50-2:00",10,"Project-integration brief","The 'Project integration' card: the project's RAG or extraction pipeline runs behind the gateway from this week on.")],
 "proj":["The project's RAG or extraction pipeline runs behind the LiteLLM gateway with fallback and a budget cap.",
         "Prompt versions tracked like model artifacts; one version swap demonstrated.",
         "Retrieval quality inspected and chunking tuned on the project's own corpus."],
 "pit":["Embedding and querying with different models silently destroys retrieval.",
        "A gateway nobody routes through protects nothing; move every call behind it."],
},
{
 "n":11,"part":"Part V &middot; LLMOps &amp; AgentOps",
 "title":"LLM Evaluation, Guardrails & Observability",
 "obj":["Build a representative eval set and run it as a regression suite.",
        "Use LLM-as-judge with its biases named and calibrated.",
        "Trace and observe LLM applications (Langfuse): spans, tokens, cost.",
        "Apply guardrails against unsafe and injected behaviour."],
 "lec":[("0:00-0:10",10,"Recap & objectives",
            ["Retrieval: what chunking controls; what the gateway centralises.",
             "Today: how you know the LLM feature works, keeps working, and resists abuse."]),
        ("0:10-0:25",15,"Motivation: 'looks good' is not evaluation",
            ["Story: the prompt tweak that improved the demo and silently broke the long tail; nobody had an eval set to catch it.",
             "LLM outputs are free text: no exception is thrown when quality drops.",
             "The discipline transfer: tests (week 3), monitoring (week 8), now evals; same idea, new layer."]),
        ("0:25-0:50",25,"Evaluation that means something",
            ["The eval set: representative inputs with expected properties; built from real usage, refreshed as usage shifts.",
             "Metrics for RAG: faithfulness (is the answer grounded?), answer relevance, retrieval recall.",
             "Golden answers versus property checks; when each applies.",
             "Eval-driven development: every prompt or model change runs the evals; a drop blocks the merge like a failing test.",
             "Board work: design five eval items for the chatbot use case, live."]),
        ("0:50-1:10",20,"LLM-as-judge, used honestly",
            ["Why judges scale where humans do not; where they break.",
             "The biases named: position, verbosity, self-preference.",
             "Calibration: agree the judge with human labels on a sample before trusting it at scale.",
             "Cheap tier for judging, flagship for generation: the cost asymmetry that makes evals affordable."]),
        ("1:10-1:20",10,"Break",None),
        ("1:20-1:40",20,"LLM observability",
            ["Tracing an LLM request: prompt version, retrieved context, model, tokens, cost, latency, as one span tree (Langfuse).",
             "Token and cost dashboards per feature and per user; the week-9 spreadsheet becomes live telemetry.",
             "Sampling production traffic into the eval set: the flywheel that keeps evals representative.",
             "Week 4's observability lesson, one layer up: same pillars, new signals."]),
        ("1:40-1:55",15,"Guardrails & injection (predict, then run)",
            ["Prompt injection: untrusted content that rewrites your instructions; the retrieved document that says 'ignore your rules'.",
             "Predict: will our chatbot follow a malicious instruction embedded in a retrieved document? Then we run it.",
             "Defense layers: input/output filtering, PII handling, instruction hierarchy, least privilege on what the model can trigger.",
             "Caching as both a cost and a latency guardrail."]),
        ("1:55-2:00",5,"Wrap-up & practice preview","Practice builds each project's eval harness, wires tracing, and attacks the service.")],
 "miss":("LLM-as-judge is objective.",
         "A judge model carries position, verbosity, and self-preference biases. It must be calibrated against human labels and used with controls, not trusted blindly."),
 "checks":[("Name two biases of an LLM judge.",
            "Position bias (favouring the first answer), verbosity bias (favouring longer answers), and self-preference (favouring its own style); calibrate against human labels before trusting it."),
           ("What is prompt injection and one defense?",
            "Untrusted input that overrides the system instructions. Defenses include input/output filtering, instruction hierarchy, and not acting on instructions embedded in untrusted content.")],
 "take":["The eval set is the regression suite for prompts; run it on every change.",
         "Calibrate the judge against humans; never trust it blind.",
         "Trace every LLM call: prompt version, context, tokens, cost, latency."],
 "prac":[("0:00-0:10",10,"Setup & recap",
            ["Open the project's RAG service and the tracing dashboard.",
             "Recap: faithfulness, judge biases, injection."]),
        ("0:10-0:35",25,"Build the eval harness",
            ["Build a 30-item starter eval set live (each team extends to 50+ on its own corpus).",
             "Score faithfulness and answer relevance (Ragas or a calibrated judge).",
             "Log the eval run in MLflow like any experiment (week 7's discipline)."]),
        ("0:35-1:00",25,"Regression in action",
            ["Change the prompt; re-run the evals; read the regression like a failing test.",
             "Calibrate the judge on ten human-labelled items; measure the agreement.",
             "Wire the eval run into CI so a quality drop blocks the merge."]),
        ("1:00-1:10",10,"Break",None),
        ("1:10-1:35",25,"Trace and observe",
            ["Wire Langfuse tracing through the gateway: every call now records prompt version, tokens, cost, latency.",
             "Read a full span tree for one RAG request; find the latency hotspot.",
             "Build the per-feature cost view; compare against the week-9 estimate."]),
        ("1:35-1:50",15,"Attack it",
            ["Plant an injection in a retrieved document; watch the unguarded service comply.",
             "Add input/output guardrails; re-attack; compare.",
             "Add the response cache; measure the cost and latency drop on repeated queries."]),
        ("1:50-2:00",10,"Project-integration brief","The 'Project integration' card: eval set, tracing, and a guardrail land on the project's LLM path this week.")],
 "proj":["An eval set of at least 50 items exists; the first eval run is logged and wired into CI.",
         "Langfuse tracing live on the project's LLM path: prompt version, tokens, cost, latency per request.",
         "One guardrail in place and demonstrated against a real injection attempt; response cache measured."],
 "pit":["A 10-item eval set measures noise; build a representative one.",
        "Never put secrets or PII in prompts or traces; scrub at the boundary."],
},
{
 "n":12,"part":"Part V &middot; LLMOps &amp; AgentOps",
 "title":"Agents & AgentOps: Tools, MCP & Managed Agents",
 "obj":["Explain the agent loop and tool use (function calling), from zero.",
        "Build and trace a tool-using agent; evaluate it at the step level.",
        "Bound agents with step, cost, and permission limits.",
        "Compare self-built agents with managed agent services."],
 "lec":[("0:00-0:10",10,"Recap & objectives",
            ["Retrieval: two judge biases; what an eval set is for.",
             "Today: from answering to acting. No prior agent experience assumed."]),
        ("0:10-0:25",15,"Motivation: the loop that would not stop",
            ["An agent is an LLM in a loop with tools: it can search, call APIs, write files, act.",
             "Story: the retry loop that mis-classified a code bug as a transient error and burned the budget overnight; bounds were the fix, not luck.",
             "Autonomy times tools equals a brand-new failure surface; that is the week."]),
        ("0:25-0:50",25,"Agents from first principles",
            ["The plan-act-observe loop drawn on the board, no framework, just the calls.",
             "Function calling precisely: the model emits a structured tool call (week 9's schemas); your code executes it and returns the observation.",
             "Tools: search, retrieval (week 10's RAG becomes a tool), calculators, internal APIs.",
             "Memory: the context window as short-term memory; external stores for long-term.",
             "Termination: the agent decides it is done, or the harness decides for it; why the second must exist."]),
        ("0:50-1:10",20,"MCP & the tool ecosystem",
            ["The integration explosion: N agents times M tools; why a standard interface was inevitable.",
             "MCP, the Model Context Protocol: servers expose tools and resources; any compliant agent can use them.",
             "Adoption across vendors; the server catalog as the live map of the ecosystem.",
             "Design lesson: a tool's description is a prompt; bad tool docs produce bad tool calls.",
             "Failure modes catalogued: hallucinated arguments, wrong tool choice, loops, cascade errors."]),
        ("1:10-1:20",10,"Break",None),
        ("1:20-1:40",20,"AgentOps: operating autonomy",
            ["Tracing as spans: every step, tool call, and decision is a recorded span; week 11's tracing, one level up.",
             "Step-level evaluation: judge the trajectory, not only the final answer; where did it first go wrong?",
             "Bounds, mandatory: max steps, wall-clock and cost budgets, an error classifier (config errors fail fast, transients retry).",
             "Least privilege on tools; human-in-the-loop checkpoints for irreversible actions."]),
        ("1:40-1:55",15,"Managed agents",
            ["Bedrock AgentCore and Foundry Agent Service: hosted loop, tool catalog, memory, tracing in one console.",
             "The same control-versus-toil axis from week 2, applied to autonomy.",
             "What you still own regardless: tool design, permissions, evaluation, and the bill."]),
        ("1:55-2:00",5,"Wrap-up & practice preview","Practice builds, traces, attacks, and bounds a real agent, then wraps a project tool as an MCP server.")],
 "miss":("An agent loop will stop on its own.",
         "Without explicit step, cost, and wall-clock bounds and a termination criterion, agents loop, retry, and burn budget. Bounds and an error classifier are mandatory: a retry-on-any-exception loop spins forever on a caller-side bug."),
 "checks":[("Why trace at the step level, not just the final output?",
            "Failures hide in intermediate tool calls and decisions; step traces localise exactly where the agent went wrong."),
           ("Name two bounds every agent loop needs.",
            "A maximum step or iteration cap and a wall-clock or cost budget, with an error classifier so caller-side bugs fail fast instead of retrying.")],
 "take":["An agent is an LLM in a loop with tools; autonomy is a new failure surface.",
         "MCP standardises the agent-tool interface across vendors.",
         "Trace and evaluate at the step level; bound steps, cost, and permissions."],
 "prac":[("0:00-0:10",10,"Setup & recap",
            ["Open the agent scaffold and the tracing dashboard.",
             "Recap: the loop, function calling, bounds."]),
        ("0:10-0:35",25,"Build the loop, no magic",
            ["Give the scaffold two tools: the project's retrieval (week 10) and a calculator or internal API.",
             "Run real tasks; read the raw model-emitted tool calls to demystify the loop.",
             "Watch a wrong-tool choice happen naturally; discuss why the description invited it."]),
        ("0:35-1:00",25,"Trace it, break it, find it",
            ["Extend the week-11 tracing to agent spans; read a successful run end to end.",
             "Trigger a failure (a hallucinated argument); localise it in the trace to the exact step.",
             "Step-level eval: write two checks on the trajectory, not the answer."]),
        ("1:00-1:10",10,"Break",None),
        ("1:10-1:35",25,"Bound it",
            ["Add the max-step cap, the cost budget, and the error classifier.",
             "Unleash a designed runaway task; watch the bounds catch it and the classifier fail fast on a config error.",
             "Add one human-in-the-loop checkpoint before an irreversible action."]),
        ("1:35-1:50",15,"MCP & the managed tour",
            ["Wrap one project tool as an MCP server; connect the agent to it.",
             "Stretch: connect to a neighbouring team's MCP server.",
             "Five-minute managed-agent console tour; map every concept you just built onto it."]),
        ("1:50-2:00",10,"Project-integration brief","The 'Project integration' card: one agentic capability, traced and bounded, lands in the project before the final hardening week.")],
 "proj":["One agentic capability added to the project (e.g. the chatbot answers via tools, the IoT system triages an alert, the extraction pipeline self-checks).",
         "Tracing on every step; a step cap, cost budget, and error classifier in force.",
         "One project tool exposed as an MCP server; stretch: consume another team's server."],
 "pit":["Broad tool permissions are dangerous; grant least privilege.",
        "A retry-on-any-exception loop spins on caller bugs; classify the error first."],
},
{
 "n":13,"part":"Part VI &middot; Security &amp; Governance",
 "title":"Security, Governance & Synthesis",
 "obj":["Secure the software, data, and model supply chain.",
        "Apply the OWASP Top 10 for LLM applications.",
        "Synthesise the five layers into one operational stack."],
 "lec":[("0:00-0:10",10,"Recap & objectives",
            ["Retrieval: two mandatory agent bounds.",
             "Today: securing and governing the whole stack, then the synthesis; final Student Project Presentations this afternoon."]),
        ("0:10-0:25",15,"Motivation: the widened attack surface",
            ["AI systems add new attack surfaces: the data, the model, the prompts, and the agent's tools are all inputs an adversary can shape.",
             "Story: a poisoned public dataset and a backdoored checkpoint; provenance was the missing control.",
             "Governance is the same widening, seen from the auditor's chair."]),
        ("0:25-0:50",25,"Securing the supply chain",
            ["Secrets management done right: vaults, rotation, least privilege; the course-long rule explained in full.",
             "Dependencies and SBOMs: knowing what you ship; pinning as a security control, not just reproducibility.",
             "The model and data supply chain: provenance of datasets and weights; signature and hash verification.",
             "CI as a security boundary: the pipeline that gates the merge also gates what reaches production."]),
        ("0:50-1:10",20,"The OWASP Top 10 for LLM applications",
            ["Prompt injection (met in week 10) and insecure output handling: never execute or render model output unexamined.",
             "Excessive agency: week 11's least-privilege rule, formalised.",
             "Sensitive-information disclosure, data poisoning, model denial-of-service (the token bill as an attack).",
             "Walk the project architectures against the list, live: every team finds at least one gap."]),
        ("1:10-1:20",10,"Break",None),
        ("1:20-1:40",20,"Governance & responsible operations",
            ["Privacy and PII across the stack: in the lake (week 5), in prompts and logs (week 9), in traces (week 11).",
             "Audit trails end to end: which data, model, prompt, and approval produced this decision?",
             "Model cards and documentation that stay live; the NIST AI RMF as the umbrella framing.",
             "Compliance as a design input, not a retrofit."]),
        ("1:40-1:55",15,"Synthesis: the five layers, one system",
            ["The full architecture on one board: lake, pipelines, registry, serving endpoint, gateway, evals, agent, telemetry, and the audit trail through all of it.",
             "Each of the five layers' one-sentence contract, recited by the class, not the instructor.",
             "What to learn next: deeper cloud, platform engineering, the research frontier in evals and agents."]),
        ("1:55-2:00",5,"Wrap-up & Student-Project-Presentation logistics","Final-presentation running order; the oral defense format; what 'done' means."),],
 "miss":("Security and governance are a final checklist.",
         "They are cross-cutting properties designed in from week one: secrets, least privilege, data contracts, and audit trails. Bolting them on at the end does not work."),
 "checks":[("Name one supply-chain risk unique to ML and LLM systems.",
            "A poisoned or backdoored pretrained model or dataset pulled from a public hub; verify provenance and pin versions."),
           ("Why keep audit trails across the whole stack?",
            "For compliance, incident forensics, and reproducibility: to answer which data, model, and prompt produced a decision, and who approved it.")],
 "take":["Security and governance are designed in from week one, not appended.",
         "The OWASP LLM Top 10 is the working checklist for LLM-app security.",
         "The five layers compose into one auditable operational stack."],
 "prac":None,
 "proj":["Final hardening before the demo: the OWASP LLM Top 10 pass on the project, with at least one found gap closed.",
         "The cost/latency report finalised from the gateway and dashboard data.",
         "Repository tagged v1.0: the end-to-end system, its docs, and its audit trail."],
 "pit":["Secrets in client-side code or logs leak; keep them server-side and rotated.",
        "A model card nobody maintains is theatre; keep documentation live."],
 "pres":"Student Project Presentation 3 &middot; Final",
 "present":["End-to-end production demo of the chosen use case: data in, decision out, live.",
            "Observability shown working: the RED dashboard, the drift monitor, and one actionable alert with its runbook.",
            "Evaluation, guardrails, and the cost/latency report (the token economy of the feature).",
            "Security and governance review against the OWASP LLM Top 10, plus a retrospective.",
            "Short oral defense: any team member answers on any part of the system."],
},
]

def render_timeline(rows):
    out = ['<table class="timeline">']
    for clock, mins, head, detail in rows:
        m = f'{mins} min' if mins else ''
        cell = f'<b>{esc(head)}</b>'
        if isinstance(detail, list):
            cell += '<ul class="dt-list">' + ''.join(f'<li>{esc(x)}</li>' for x in detail) + '</ul>'
        elif detail:
            cell += f'<span class="dt">{esc(detail)}</span>'
        out.append(f'<tr><td class="tm">{clock}</td><td class="mn">{m}</td><td>{cell}</td></tr>')
    out.append('</table>')
    return ''.join(out)

def page(w, prev, nxt):
    nn = f'{w["n"]:02d}'
    title = esc(w["title"])
    obj = ''.join(f'<li>{esc(x)}</li>' for x in w["obj"])
    checks = ''.join(
        f'<details><summary>{esc(q)}</summary><div class="ans">{esc(a)}</div></details>'
        for q, a in w["checks"])
    take = ''.join(f'<li>{esc(x)}</li>' for x in w["take"])
    pit = ''.join(f'<li>{esc(x)}</li>' for x in w["pit"])
    proj = ''.join(f'<li>{esc(x)}</li>' for x in w["proj"])
    pres_badge = (f' &nbsp; <span class="wbadge presb">&#127908; {w["pres"]}</span>'
                  if w.get("pres") else '')

    if w.get("pres"):
        present = ''.join(f'<li>{esc(x)}</li>' for x in w["present"])
        practice_html = (
            '<h2 id="practice"><span class="ic">&#127908;</span>'
            '<span class="secttag presi">Student Project Presentation &middot; 2 hours</span></h2>'
            f'<p>The full two-hour practice slot is given over to <b>student project presentations ({w["pres"]})</b>. '
            'There is no instructor-prepared material: teams present and defend their work to the class, with peer '
            'and instructor questions after each talk. Each team has 12 to 15 minutes plus questions, and submits a '
            'short written report and a tagged release of the repository.</p>'
            '<div class="callout check"><b>What each team presents.</b>'
            f'<ul class="clean" style="margin-bottom:0">{present}</ul></div>'
            '<p style="margin-top:10px">See the <a href="../index.html#project">running-project brief</a> for the '
            'full milestone description and the grading weight.</p>')
    else:
        practice_html = (
            '<h2 id="practice"><span class="ic">&#128187;</span>'
            '<span class="secttag prac">Practice &middot; 2 hours</span></h2>'
            '<p>In the practice session the instructor demonstrates the tooling live and teaches the hands-on '
            'topics that belong at the keyboard. There are no separate weekly labs: each session closes with the '
            'project-integration brief, the increment every team adds to its end-to-end system before next week.</p>'
            f'{render_timeline(w["prac"])}'
            f'<div class="callout hint"><b>Common pitfalls to pre-empt.</b>'
            f'<ul class="clean" style="margin-bottom:0">{pit}</ul></div>')

    extra_pit = ''
    if w.get("pres"):
        extra_pit = (f'<div class="callout hint"><b>Common pitfalls to pre-empt.</b>'
                     f'<ul class="clean" style="margin-bottom:0">{pit}</ul></div>')

    proj_html = (f'<div class="goals"><h2>Project integration (this week)</h2>'
                 f'<ul class="clean" style="margin-bottom:0">{proj}</ul></div>')

    pager = []
    if prev:
        pager.append(f'<a href="week{prev["n"]:02d}.html"><span class="lbl">Previous</span>Week {prev["n"]}: {esc(prev["title"])}</a>')
    if nxt:
        pager.append(f'<a class="nx" href="week{nxt["n"]:02d}.html"><span class="lbl">Next</span>Week {nxt["n"]}: {esc(nxt["title"])}</a>')
    pager_html = f'<div class="pager">{"".join(pager)}</div>' if pager else ''

    return f"""<!DOCTYPE html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title>Week {w['n']} Lesson Plan: {title}</title>
<link rel="stylesheet" href="../assets/style.css"></head><body>
<div class="topnav"><div class="inner">
<a class="brand" href="../index.html">{BRAND} <span>&middot; HIT</span></a>
<nav><a href="../index.html">Home</a><a href="../prereq/index.html">Prerequisites</a><a href="../index.html#weekly">Weekly materials</a><a href="../index.html#project">Project</a><a href="../references/index.html">References</a></nav>
</div></div>
<div class="wrap">
<div class="whead">
<p class="eyebrow"><span class="wbadge">Week {w['n']}</span> &nbsp; {w['part']}{pres_badge}</p>
<h1>{title}</h1>
<p class="sub">Instructor lesson plan: lecture (2 h) and practice (2 h).</p>
</div>

<div class="goals"><h2>Learning objectives</h2><ul class="clean">{obj}</ul></div>

<h2><span class="ic">&#127891;</span><span class="secttag">Lecture &middot; 2 hours</span></h2>
{render_timeline(w['lec'])}

<div class="callout miss"><b>Common misconception to confront.</b>
<p style="margin:6px 0 0"><i>Students often think:</i> {esc(w['miss'][0])}<br>
<i>Set it straight:</i> {esc(w['miss'][1])}</p></div>

<div class="callout check"><b>Check for understanding</b> (pose during the concept blocks; let students answer before revealing).
<div class="selfcheck" style="margin-top:8px">{checks}</div></div>

<div class="callout"><b>Key takeaways.</b><ul class="clean" style="margin-bottom:0">{take}</ul></div>
{extra_pit}

{practice_html}

{proj_html}

<p style="margin-top:22px"><a class="btn" href="../references/index.html#w{nn}">Curated references</a> <a class="btn" href="../index.html#project">Project brief</a></p>
{pager_html}
</div>
<footer>{FOOT}</footer>
</body></html>
"""

def main():
    for i, w in enumerate(WEEKS):
        prev = WEEKS[i-1] if i > 0 else None
        nxt = WEEKS[i+1] if i < len(WEEKS)-1 else None
        path = os.path.join(OUT, f'week{w["n"]:02d}.html')
        with open(path, "w", encoding="utf-8") as f:
            f.write(page(w, prev, nxt))
        print(f'wrote {path}')
    print(f'done: {len(WEEKS)} lesson plans')

if __name__ == "__main__":
    main()
